.row
  .col-12
    %h3 Formats
    %p
      This community has a growing number of formats. These are the primary ones played:
    %ul
      - @formats.sort_by(&:format_pretty_name).each do |format|
        %li
          = link_to format.format_pretty_name, action: "show", id: format.format_pretty_name.parameterize
